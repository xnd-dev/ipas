export const breaks = {
  xs: 480,
  sm: 768,
  md: 1024,
  lg: 1200,
}

export const media = {
  xs: `(max-width: ${breaks.xs}px)`,
  sm: `(max-width: ${breaks.sm}px)`,
  md: `(max-width: ${breaks.md}px)`,
  lg: `(max-width: ${breaks.lg}px)`,
}
